# here would be the code for changing up the agents, environments, 
# and interactions between everything 

def agents(type_of_agent, social_distancing_factor, infected):
    self.infected = infected 
    self.type = type_of_agent
    self.sdf = social_distancing_factor

def environment(num_agents, space):
    